# ============================================================
# TOP GRADE RICE MILLERS — PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================

# Secret key used to sign administrative session cookies (HMAC-SHA256).
# Generate with: openssl rand -base64 32
SESSION_SECRET=replace_with_a_cryptographically_secure_random_string_in_production

# Node environment (production | development)
NODE_ENV=production

# Base URL for metadata, canonical links and sitemap
NEXT_PUBLIC_SITE_URL=https://www.topgradericemillers.co.ke
